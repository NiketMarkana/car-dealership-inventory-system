{
  "name": "car-dealership-inventory-system-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend",
    "test": "npm run test --prefix backend"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint-config-prettier": "^10.1.8"
  }
}
